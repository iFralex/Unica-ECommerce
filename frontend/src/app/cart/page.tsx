import { getCartFromCartLight, getCookie } from "@/actions/get-data"
import { getCartLight, pushCartData } from "@/actions/firebase"
import { CartClient } from "./client"

const Page = async () => {
   
    return <CartClient><></></CartClient>
}

export default Page